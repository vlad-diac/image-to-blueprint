services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: blueprint
    ports:
      - '5454:5432'
    volumes:
      - blueprint_pg:/var/lib/postgresql/data

volumes:
  blueprint_pg:
